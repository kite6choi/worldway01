# 월드웨이 동결건조기 예지보전 AI 웹 애플리케이션 (PWA) 요구사항 정의서 (spec.md)

## 1. 프로젝트 개요

본 프로젝트는 **월드웨이(Worldway)**와 **엔씨씨(NCC)**의 제조데이터 상품가공 지원사업을 통해 설계된 **'진공 동결 건조기 예지보전 하이브리드 AI 알고리즘'**을 산업 현장에서 실시간으로 시연, 검증, 모니터링할 수 있도록 클라이언트 사이드 기반의 **PWA(Progressive Web App)** 형식으로 개발하는 시스템입니다.

설비의 극심한 데이터 불균형 문제를 극복하기 위해 제안된 **[1단계: 비지도 학습(Autoencoder) 이상 탐지] ➡ [2단계: 지도 학습(XGBoost) 고장 진단]** 계단식 하이브리드 아키텍처를 웹 브라우저 상에서 100% 로컬(Client-side) 연산으로 구동하며, 최종 예지 성능 지표인 **F1-Score 0.80(80%) 이상**의 달성 여부를 직관적으로 시뮬레이션하고 검증할 수 있는 환경을 제공합니다.

---

## 2. 목표 및 핵심 가치 (Core Value)

*   **3분할(Train-Val-Test) 검증 엄격성**: 최종 수능 시험 역할을 할 50,000건의 시험 데이터셋을 완전 격리하여 데이터 누수(Data Leakage)를 방지하고, 80% F1-Score 검증의 공인적 객관성을 확보합니다.
*   **하이브리드 추론의 제어 루프 정형화**: 오토인코더 복원 오차가 임계치선 이하일 때는 빠르게 1단계에서 연산을 종결하고, 이를 초과하는 변칙 상황에서만 다중 분류기를 구동해 고장 원인을 진단합니다.
*   **실시간 데이터 생성형 시뮬레이션**: 1초 간격으로 시계열 센서 데이터를 생성하되, 10%의 고정 확률로 실제 고장 벡터를 인젝션(Fault Injection)하여 그래프의 알람 상태 전이(정상 ➡ 주의 ➡ 이상)를 실시간 시동 시연합니다.
*   **현장 모바일 최적화**: PWA 표준 규격을 준수하여 오프라인 상황에서도 동작하며, 현장 스마트 패드와 모바일에서 접근 가능한 반응형 UI를 보장합니다.
*   **100% 로컬 프라이버시 보호**: 공장의 민감한 제조 데이터를 외부 서버에 업로드하지 않고, 사용자의 웹 브라우저 내 메모리에서 전량 가공 및 AI 추론을 수행하여 기밀 유출을 원천 차단합니다.

---

## 3. 대상 데이터셋 및 3분할(Train-Val-Test) 규격

### 3.1 원시 데이터 및 입력 파일 명세
사용자는 아래의 두 실측 데이터셋(CSV)을 브라우저에 적재하여 프로그램을 구동합니다.
*   **정상 가동 데이터셋 (`챔버1_정상_12만건.csv`)**
    *   규격: 챔버 1호기의 정상 건조 배치 사이클 동안 30초 주기로 수집된 무결점 시계열 데이터. (120,000건, `label_class = 0`)
*   **고장 가동 데이터셋 (`챔버1_고장전1주일데이터_3만건.csv`)**
    *   규격: 설비 고장 발생 전 특정 골든타임 구간 및 고장 이벤트 시점이 기록된 변칙 데이터. (30,000건, 드라이펌프/부스터1/부스터2 고장 각 10,000건 포함, `label_class = 1, 2, 3`)
*   **독립 변수 (Features - 5개)**: `드라이펌프 (A)`, `부스터1 (A)`, `부스터2 (A)`, `열매in온도 (℃)`, `진공 (Torr)`
*   **정답 라벨 (Target - 4개 클래스)**: `정상(Class 0)`, `드라이펌프고장(Class 1)`, `부스터1고장(Class 2)`, `부스터2고장(Class 3)`

### 3.2 층화 분할(Stratified Split) 상세 매트릭스
데이터의 불균형을 예방하고, 오토인코더 조기 종료용 검증 데이터 및 최종 평가 시험 데이터를 철저하게 보호하기 위해 아래의 **[학습(8만) : 검증(2만) : 시험(5만)]** 비율로 데이터셋을 층화 분할합니다.

| 분류 구분 (Label) | 전체 데이터 수량 | 순수 학습 데이터 (Train - 53.3%) | 학습 검증 데이터 (Val - 13.3%) | 최종 시험 데이터 (Test - 33.3%) |
| :--- | :---: | :---: | :---: | :---: |
| **정상 가동 (Class 0)** | 120,000건 | **64,000건** | **16,000건** | **40,000건** |
| **드라이펌프고장 (Class 1)** | 10,000건 | **5,333건** | **1,333건** | **3,334건** |
| **부스터1고장 (Class 2)** | 10,000건 | **5,333건** | **1,333건** | **3,334건** |
| **부스터2고장 (Class 3)** | 10,000건 | **5,334건** | **1,334건** | **3,332건** |
| **합계 (Total)** | **150,000건** | **80,000건** | **20,000건** | **50,000건** |

---

## 4. 단계별 AI 알고리즘 및 학습 프로세스

### **[1단계] 오토인코더(Autoencoder) 비지도 이상 탐지 훈련**
1.  **훈련 데이터**: 학습용 데이터셋(80,000건) 중에서 **오직 정상 데이터 64,000건만 선별**하여 입력합니다.
2.  **검증 데이터**: 검증용 데이터셋(20,000건) 중에서 **정상 데이터 16,000건**을 검증(Validation) 세트로 물려 훈련 시 손실률(Loss)을 모니터링합니다.
3.  **조기 종료(Early Stopping)**: 검증 정상 오차의 에포크별 향상이 더 이상 없을 때 과적합 방지를 위해 자동으로 학습을 중단시킵니다.
4.  **임계치(Threshold) 수치 연산**: 학습 종료 후 검증 정상 데이터 16,000건의 재구성 오차 분포에서 **상위 99% 백분위값(Percentile 99)** 또는 **3-Sigma(\(\mu + 3\sigma\))** 수치를 이상 임계치(\(T_h\))로 결정하여 프로그램에 잠금(Lock) 지정합니다.

### **[특징 융합] 10차원 지능형 피처 행렬(Feature Matrix) 생성**
1.  학습 세트의 정상 및 고장 3종이 섞여 있는 **80,000건 전체**를 오토인코더에 통과시킵니다.
2.  각 샘플당 오토인코더가 계산한 변수별 복원 오차 벡터 $e = [e_{\text{dry}}, e_{\text{b1}}, e_{\text{b2}}, e_{\text{temp}}, e_{\text{vac}}]$ 5개를 획득합니다.
3.  원시 5개 피처 옆에 복원 오차 5개 피처를 병합하여 **총 10개 피처를 갖는 80,000행의 다차원 행렬**을 최종 다중 분류기의 훈련 피처셋으로 승격시킵니다.

### **[2단계] XGBoost 기반 다중 분류 고장 진단 학습**
1.  앞서 가공된 **10차원 융합 학습 행렬(80,000건)**과 4대 상태 정답 라벨을 결합하여 다중 분류기(XGBoost / Random Forest) 모델을 학습시킵니다.
2.  이때 검증용으로 분할했던 **20,000건(오토인코더를 통해 동일하게 10차원으로 융합 가공)**을 분류기의 최적 파라미터 튜닝 검증 세트로 사용하여 과적합을 원천 회피합니다.

---

## 5. 기능적 요구사항 (Functional Requirements)

### F-100: 데이터 인제스천 및 전처리 (Data Ingestion & Preprocessing)
*   **F-101: 로컬 파일 드래그 앤 드롭 업로드**
    *   사용자가 PC에 보관 중인 대용량 가동 데이터(`챔버1_정상_12만건.csv` 및 `챔버1_고장전1주일데이터_3만건.csv` 파일)를 브라우저 화면에 끌어다 놓아 로컬 세션에 로드할 수 있어야 합니다.
*   **F-102: 시계열 시간 축 동기화 (Time Alignment) 및 전처리**
    *   결측치를 선형 보간(Linear Interpolation) 처리하여 완전히 동기화된 가공 행렬로 병합합니다.
    *   진공도가 대기압 상태(예: 10 Torr 초과)이거나 펌프 전류가 차단된 '배치 대기 상태'는 분석 대상에서 제외하고 유효 운전 영역만 정제합니다.

### F-200: 실시간 1초 주기 데이터 스트림 제어기
*   **F-201: 1초 단위 자동 플레이백 (Data Generator)**
    *   사용자가 시뮬레이션을 작동시키면, 시스템은 **1초에 1건씩** 시계열 가동 데이터를 로컬 메모리 버퍼에서 순차적으로 생성하여 하이브리드 추론 엔진에 주입합니다.
*   **F-202: 10% 확률의 고장 인젝션 (Fault Injection)**
    *   시뮬레이션의 동적 시연을 위해, 매 1초 데이터 생성 시 **약 10%의 난수 확률**로 드라이펌프/부스터1/부스터2 고장 클래스의 데이터 벡터를 강제로 주입합니다.

### F-300: 다중 융합 동적 시각화 대시보드
*   **F-301: 실시간 센서 트렌드 멀티 라인 그래프**
    *   입력 데이터에 연동하여 드라이펌프, 부스터1/2 전류, 진공, 온도가 우측으로 스크롤되며 실시간 렌더링되는 시계열 꺾은선 그래프를 제공합니다.
*   **F-302: 실시간 오토인코더 오차 모니터링 그래프**
    *   오토인코더 복원 오차의 변동 폭과 고정 설정된 **이상 임계치선(Threshold Line)**을 함께 겹쳐서 가시화합니다. 오차가 이 기준선을 뚫고 솟아오르면 즉시 시각적으로 알람이 트리거됩니다.
*   **F-303: 실시간 고장 원인 확률(%) 누적 면적/바 차트**
    *   임계치 돌파 시 실시간 구동되는 XGBoost 분류기가 산출해낸 **4대 클래스의 발생 예상 확률(%)**을 실시간 바 차트 또는 누적 시계열 차트로 표시합니다 (예: 부스터1 고장 확률 80%).
*   **F-304: 3색 경보 신호등 뷰**
    *   정상 상태 시 녹색 테두리 및 "안정 운전", 주의 및 단순 오차 돌출 시 황색, 임계치 장기 초과 및 확실 고장 판정 시 적색 사이렌 경고와 함께 "부스터1 긴급 점검 필요 (진단 신뢰도 80%)\" 등의 한글 대처 정비 가이드를 가시화합니다.

### F-400: TDD 기반 정량 검증 판넬
*   **F-401: 실시간 Confusion Matrix 업데이트**
    *   시험 데이터 50,000건 시뮬레이션 중, 생성된 데이터의 실제 정답 라벨과 하이브리드 예측 모델의 판단 결과를 상시 대조하여 **혼동 행렬(Confusion Matrix) 매트릭스를 실시간 수치 표로 갱신**합니다.
*   **F-402: 공인 F1-Score 대조기**
    *   정밀도(Precision), 재현율(Recall), 종합 **F1-Score**를 지속 업데이트하며, 사업 필수 품질 기준인 **`0.80`** 돌파 여부를 초과/달성 이미지 뱃지로 직관적으로 표시합니다.

---

## 6. 비기능적 요구사항 (Non-Functional Requirements)

### N-100: 성능 및 응답성
*   **N-101: 60fps 캔버스 최적화**: 1초 간격의 차트 스크롤 및 렌더링 시 브라우저 메인 스레드 점유율을 5% 미만으로 억제하기 위해 Chart.js의 캔버스 가속 모드 또는 uPlot을 채택합니다.
*   **N-102: 오프라인 완전 가용**: `service-worker.js`를 통해 앱 리소스(HTML, CSS, JS, AI 가중치 바이너리)를 완벽히 로컬 캐싱하여 인터넷이 차단된 지하 기계실에서도 가동하도록 설계합니다.

### N-200: 기술 스택 및 무서버 정적 배포
*   **N-201: 기술 스택**: TensorFlow.js (오토인코더 학습/추론), Scikit-learn/XGBoost를 JS JSON 형식으로 이식한 ML 라이브러리, CSS Tailwind + DaisyUI, Javascript ES6+. **Bootstrap 5 사용은 과도한 번들 크기와 다크 테마 커스텀 복잡성으로 인해 배제**합니다.
*   **N-202: Netlify CDN 배포**: 백엔드 API나 DB 의존성 없이 프론트엔드 단독 소스 구조로 설계되어 Netlify에 static 폴더 등록 즉시 전 세계 라이브 가용성을 보장합니다.
